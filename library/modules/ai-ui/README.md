# AI UI Module

This module owns CSMA-specific AI UI composition from structured AI output.

Responsibilities are split as follows:

- `library/modules/ai/`
  - provider/backend/LLM orchestration
- `library/modules/ai-ui/`
  - runtime CSMA UI composition, archetype compilation, and view exposure

Keep all `ai-ui` implementation here.
